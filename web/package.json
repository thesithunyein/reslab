{
  "name": "@reslab/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ResLab product site: landing page + live in-browser demo + CSV analysis, powered by @reslab/core.",
  "scripts": {
    "dev": "vite",
    "build": "npm run build -w @reslab/core && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@reslab/core": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
